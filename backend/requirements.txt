# Stack de backend — ver aether_context_docs.md, sección 5.
# SQLite (desarrollo) no necesita driver aparte: viene en la librería estándar
# de Python. No se instala driver de PostgreSQL todavía porque esa decisión
# ("candidato a escalar") no está cerrada — evitar dependencias especulativas
# (principio 4.5).

fastapi
uvicorn[standard]
sqlalchemy

# Dependencias del modo demo de escritorio (streaming de cámara en
# GET /perception/stream), no de la arquitectura de producción real — en el
# Jetson real la percepción vive en edge/, no en backend/. Ver el comentario
# junto a ese endpoint en main.py para el detalle completo.
opencv-contrib-python
ultralytics
pyzbar
